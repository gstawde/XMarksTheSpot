-- Drop database schema if exists
-- DROP SCHEMA IF EXISTS x_marks_the_spot;

-- Create a new database schema
CREATE SCHEMA IF NOT EXISTS xmarksthespot$default;

-- Drop exist user
-- DROP USER IF EXISTS 'x_marks_the_spot'@'localhost';

-- Create new user
-- CREATE USER 'x_marks_the_spot'@'localhost' IDENTIFIED BY 'x_marks_the_spot';

-- Grant privileges for this user to new schema
-- GRANT ALL PRIVILEGES ON x_marks_the_spot.* TO 'x_marks_the_spot'@'localhost';